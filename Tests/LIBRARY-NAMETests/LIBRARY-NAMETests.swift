@testable import LIBRARY-NAME
import XCTest

final class LIBRARY-NAMETests: XCTestCase {
    func testExample() {
        XCTAssertEqual(LIBRARY-NAME().test(), "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample)
    ]
}
